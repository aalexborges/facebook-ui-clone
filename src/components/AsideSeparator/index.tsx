import { memo } from 'react'

import { Container, Content } from './styles'

export const AsideSeparator = memo(function AsideSeparator() {
  return (
    <Container>
      <Content />
    </Container>
  )
})
