import styled from 'styled-components'

export const Container = styled.div`
  width: 100%;
  height: 100vh;
`

export const Main = styled.main`
  position: relative;
  padding-top: 72px;

  display: flex;
  flex-direction: row;
`
